using System;

namespace Csk.Palindrome
{
    public class StringPalindromeChecker
    {
        /// <summary>
        /// Return true if a string is a palindrome, which is a string
        /// that reads the same forwards or backwards. 
        /// 
        /// Example: "abba" is a palindrome, "abbb" is not.
        ///
        /// </summary>
        /// <param name="pString">The string value to check (not-null)</param>
        /// <returns></returns>
        public bool IsPalindrome(string pString)
        {
            return false;
        }
    }
}
